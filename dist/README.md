# dist

`PIDTuner.exe` is the local PyInstaller build output.

The executable is about 516 MB, so it is not committed as a normal Git file.
Rebuild it with:

```bat
build_exe.bat
```

For public distribution, upload `PIDTuner.exe` to a GitHub Release or use Git LFS.
